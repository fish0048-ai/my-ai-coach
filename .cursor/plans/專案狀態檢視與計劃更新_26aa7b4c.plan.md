# 專案狀態檢視與計劃更新

## 三、未來擴展功能（待規劃）

### 3.1 多平台 API 同步

**功能描述**:

- 透過 Strava、Garmin Connect、Apple Health 官方 API 自動同步資料
- 需要申請各平台 API 權限和 OAuth 流程

**實現要點**:

- 擴展 `src/services/import/platformSync.js` 的 `syncFromPlatformAPI()` 函數
- 實現 OAuth 授權流程
- 添加同步設定頁面
- 需要申請各平台 API 權限

**預期收益**: 提高資料匯入便利性，實現自動同步

---

### 3.2 PDF 報告生成

**功能描述**:

- 使用 jsPDF 生成訓練報告 PDF
- 需要安裝 jsPDF 依賴

**實現要點**:

- 安裝 `jspdf` 套件
- 擴展 `src/utils/reportGenerator.js` 添加 PDF 生成功能
- 在分享選單中添加「下載 PDF 報告」選項

**預期收益**: 提供更專業的報告格式

---

### 3.3 定期備份提醒

**功能描述**:

- 定期提醒用戶備份資料
- 可設定提醒頻率

**實現要點**:

- 在 `backupService.js` 中添加提醒邏輯
- 使用瀏覽器通知 API 或應用內提醒
- 記錄最後備份時間

**預期收益**: 提升用戶資料安全性

---

## 四、技術債務與優化

### 

### 待改進項目

- [ ] 添加 E2E 測試（Playwright 或 Cypress）
- [ ] 完善 README.md 文檔
- [ ] TypeScript 遷移（可選）
- [ ] 性能監控和錯誤追蹤（Sentry）
- [ ] CI/CD 流程完善

---

## 五、實施建議

### 

### 未來擴展項目

1. **多平台 API 同步** - 需要申請 API 權限和實現 OAuth 流程
2. **PDF 報告生成** - 需要安裝 jsPDF 套件
3. **定期備份提醒** - 可選功能，提升用戶體驗

---

## 六、未來專業化功能（待規劃）

以下兩個方向可進一步提升產品的專業度與 AI 智能深度，建議作為下一階段演進目標。

### 6.1 比賽配速策略計算器（Race Strategy Generator）

**理由（Math/Performance）**：

- 針對「半馬破 2」這類目標，單純的平均配速（Average Pace）是不夠的
- 實務上會採用 Negative Split（負分段）、補給時機、賽道起伏來調整策略

**功能描述**：

- **輸入**：
- 目標成績（例如：`1:59:00`）
- 賽道類型（平路 / 起伏）
- 可選：個人 FTP / 閾值配速、歷史比賽成績
- **輸出策略**：
- Negative Split（負分段）策略：
- 例如：前 10K 配速 5:45/km，後 11K 配速 5:35/km
- 可根據使用者耐力曲線與過往訓練自動微調
- 補給策略：
- 結合現有營養模組，在時間軸上標記「能量膠攝取點」
- 例如：比賽第 45 分鐘、第 90 分鐘，自動對應到距離與補給提醒
- **輸出格式**：
- 可視化表格：每 1K / 每 5K 的目標時間、累積時間、區間配速
- 圖表：配速隨距離變化的折線圖，標出關鍵分段
- 「配速手環」PDF：
- 產生可列印的配速條（每公里目標時間），方便跑者剪下貼在手上

**實現要點**：

- 在 `src/utils/workoutCalculations.js` 中新增配速策略計算函數
- 在 `TrainingPlanView` 中增加「比賽策略」子頁或彈窗，讓使用者輸入目標成績與賽道資訊
- 使用現有報表模組（`reportGenerator.js`）生成 PDF 或圖片版配速手環
- 未來可與 Calendar 整合，在比賽日前自動顯示「比賽配速策略」卡片

**預期收益**：

- 從「訓練計畫」延伸到「比賽日策略」，讓產品覆蓋完整訓練週期
- 結合現有營養模組，提供更貼近實戰的補給建議
- 提升對中高階跑者與教練的吸引力

---

### 6.2 RAG 個人化知識庫（Retrieval-Augmented Generation）

**理由（AI Engineering）**：

- 目前 Prompt Context 受限於 token 數量，無法長期記住使用者三個月前的日記、傷痛史、物理治療建議
- 若能結合 RAG（檢索增強生成），AI 教練可以在回答問題時「回顧」過去所有重要紀錄，提升連續性與信任感

**功能描述**：

- **資料來源**：
- 訓練日記（例如：Calendar 備註欄位）
- 傷痛紀錄（例如：特定 tag 或 Injury log）
- 物理治療／復健建議（使用者自填或教練輸入）
- **技術架構**：
- 引入向量搜尋（Vector Search）：
- 可選：Firebase Vector Search 擴充功能、Pinecone、Supabase Vector 等
- 將上述文字內容轉為 Embeddings（向量）並儲存
- **使用場景**：
- 當使用者問 AI：
- 例：「我左膝蓋最近有點怪怪的」
- AI 不只看本週數據，還會檢索：
- 「你兩個月前的 10/15 也有紀錄過左膝外側緊繃，當時是因為跑坡訓練過多……」
- 回覆中自動引用過往紀錄片段，讓回答具備「記憶溫度」

**實現要點**：

- 在 `aiContextService` / `contextManager` 中擴展一層 RAG 流程：
- 先用關鍵字 / Embedding 檢索出最相關的 N 筆歷史紀錄
- 再將這些紀錄作為 Context 傳給 Gemini
- 新增 `ai/knowledgeBaseService.js`（或類似命名）：
- 負責寫入 / 更新 / 刪除 Embeddings
- 封裝向量搜尋邏輯，避免洩漏具體供應商細節（Firebase / Pinecone 可替換）
- UI 層新增：
- 在教練聊天視窗中標註「本次回答參考了以下歷史紀錄：…」
- 在 Profile 或設定中，提供「匯出 / 刪除個人知識庫」的隱私控制選項

**預期收益**：

- 讓 AI 教練具備長期記憶能力，而不受單次 Prompt token 限制
- 在傷痛管理、長期計畫調整、心理狀態追蹤等情境中，提供更貼近真人教練的體驗
- 打造差異化賣點，讓產品從「通用健身 AI」進化為「真正懂你的私人教練」

---

## 七、重構 TODO 清單（以安全 Quick Wins 優先）

> 說明：本清單從「不破壞現有功能」出發，先做 3 個最安全、最有感的 Quick Wins，再列出後續可分階段執行的任務。
> 每項都標註是否適合 **自動重構**，或需較多 **人工確認**。

### ✅ Quick Win 1：統一日期與數字工具函數（formatDate / getWeekDates / cleanNumber 等）

- **目標**  
- 消除在各個 View / utils 內重複定義的日期與數字處理函數，集中到單一工具檔，之後所有邏輯統一從這裡取用。  
- **修改檔案**  
- 新增：`src/utils/date.js`（`formatDate`, `getWeekDates`, 其他常用日期工具）  
- 新增：`src/utils/number.js`（`cleanNumber`, `parsePaceToDecimal` 等數值處理）  
- 修改（只改 import 與刪重複函式）：  
- `src/views/CalendarView.jsx`  
- `src/views/TrendAnalysisView.jsx`  
- `src/utils/importHelpers.js`（若仍在使用）  
- **預估影響面**  
- 影響檔案數：3–5 個（依實際搜尋結果為準）  
- 主要變動為「替換重複函數定義 → 改成 import」，邏輯不變，**功能風險低**。  
- **完成標準**  
- [x] 全域搜尋 `formatDate(`、`getWeekDates(`、`cleanNumber(` 與 `parsePaceToDecimal(`，皆來自 `utils/date` 或 `utils/number`。  
- [x] 刪除各 View / utils 內部重複定義的同名函數。  
- [x] Calendar / TrendAnalysis 等頁面的日期顯示、配速計算與匯入流程仍正確無誤。  
- [x] 瀏覽器 console 無新增錯誤或警告。  
- **自動化程度**  
- **適合自動重構 + 人工驗證**  
- 建議流程：先把工具函數複製到 `utils` → 用「全專案取代」把舊定義刪掉、改為 import。  
- 需要人工跑一輪關鍵頁面（Calendar / TrendAnalysis）確認行為。  

### ✅ Quick Win 2：統一 API Key 管理（Gemini / 之後其他 AI）

- **目標**  
- 把目前分散在各處的 `localStorage.getItem('gemini_api_key')`、API Key 驗證邏輯統一收斂，讓後續更換儲存方式或加入多把 API Key 更容易。  
- **修改檔案**  
- 新增：`src/services/apiKeyService.js`（或 `src/services/config/apiKeyManager.js`）  
- `getApiKey()`, `setApiKey(key)`, `hasApiKey()`  
- 新增（可選）：`src/hooks/useApiKey.js`  
- 回傳 `{ apiKey, setApiKey, hasApiKey }`，給需要即時反應 UI 的地方用。  
- 修改（將直接存取 localStorage 改為呼叫 service/hook）：  
- `src/components/AICoach/CoachChat.jsx`  
- `src/services/ai/workoutGenerator.js`  
- `src/services/ai/nutritionRecommendation.js`  
- 其他使用 Gemini 的 View 或 services（例如 Strength/Run Analysis、NutritionView 等）。  
- **預估影響面**  
- 影響檔案數：約 4–8 個（依實際搜尋 `gemini_api_key` 決定）。  
- 實際行為保持不變，重點在集中邏輯，**中低風險但收益大**。  
- **完成標準**  
- [x] 全域搜尋 `localStorage.getItem('gemini_api_key')` 結果為 0。  
- [x] 所有服務改用 `apiKeyService` 或 `useApiKey` 取得 API Key。  
- [x] AI 教練、AI 課表、營養建議、動作分析等功能均可正常呼叫 Gemini。  
- [x] API Key 遺失或錯誤時，錯誤處理訊息一致且可透過 `ErrorToast` 顯示。  
- **自動化程度**  
- **部分可自動重構，多數需人工確認**  
- `localStorage.getItem('gemini_api_key')` 可用全專案搜尋替換成 `getApiKey()`。  
- 但每個呼叫點的錯誤處理、UI 交互（例如在沒有 Key 時顯示提示）需人工檢查與調整。  

### ✅ Quick Win 3：抽出共用 AI JSON 解析 Helper（處理 ```json + 雜訊字串） 

- **目標**  
- 將目前在各 AI service 裡重複出現的「清掉 ```json 標記 → 找第一個 `{`/`[` → `JSON.parse`」邏輯抽成共用 helper，降低 bug 機率並統一錯誤處理。  
- **修改檔案**  
- 新增：`src/utils/aiJson.js`（名稱可調整）  
- 例如：`parseLLMJson(raw, { rootType: 'object' | 'array' })`。  
- 修改（把內建的字串處理/parse 改用 helper）：  
- `src/services/ai/workoutGenerator.js`  
- `src/services/ai/nutritionRecommendation.js`  
- 其他會從 LLM 讀 JSON 的 services（如動作分析、教練對話若有結構化輸出）。  
- **預估影響面**  
- 影響檔案數：3–6 個 AI service。  
- 功能不變，但若 helper 寫錯會影響多處，**需要一次寫好 + 驗證**。  
- **完成標準**  
- [x] 所有 AI service 不再自行 `replace(/```json/g, '')` / `substring(startIndex, endIndex + 1)`，改由 `parseLLMJson` 處理。  
- [x] 在 AI 回傳正常 JSON、包在 ```json 區塊、或有前後雜訊文案時都能成功 parse。  
- [x] 當解析失敗時，會統一丟出可被 `handleError` 捕捉的錯誤（含 context）。  
- [x] 實測：AI 生成功能，比對重構前後輸出的結構與欄位一致。  
- **自動化程度**  
- **不適合完全自動重構，需要人工確認**  
- 雖可用搜尋找出共通 pattern，但每支 service 的輸出格式與期待 rootType 可能不同。  
- 建議：人工逐檔調整，並針對每支功能做一次實際呼叫測試。  

---

## 八、後續階段任務（依優先度拆分）

以下任務對應先前的重構路線圖，建議按「影響最大 → 風險可控」的順序執行。

### Phase 2-1：收斂 Workout / Calendar Domain（中風險）

- **目標**  
- 把「訓練紀錄/行事曆」相關的業務邏輯（包含 PR、training load、週統計）集中到一個 `workoutService`，讓 `DashboardView`、`TrainingDashboardView`、`CalendarView` 共用同一套資料邏輯。  
- **修改檔案**  
- 新增：`src/services/workoutService.js`  
- 修改：  
- `src/services/calendarService.js`（只保留最基本的 CRUD / 訂閱，複雜統計邏輯搬到 workoutService）  
- `src/views/DashboardView.jsx`  
- `src/views/TrainingDashboardView.jsx`  
- **預估影響面**  
- 影響訓練統計與儀表板顯示，屬「高可見度」區域。  
- 若日期篩選或 status 過濾邏輯有改動，可能導致數字變動，**需特別驗證**。  
- **完成標準**  
- [ ] Dashboard 與 TrainingDashboard 的統計都改成只呼叫 `workoutService`，不直接操作 `calendarService`。  
- [ ] 重構前後，對同一帳號的「近 30 天訓練次數、本週跑量、Zone2 比例、PR 資訊」數值一致。  
- [ ] Calendar 基本 CRUD 與 Dashboard 顯示保持同步。  
- **自動化程度**  
- **不適合自動重構，需要人工確認**  
- 屬於 domain 切分與邏輯移轉，建議逐步搬移並搭配手動驗證。  

### Phase 2-2：AI 功能服務化（中風險）

- **目標**  
- 讓 Calendar、Nutrition、Analysis 等 View 不再自己組 prompt，而是呼叫 `services/ai/*` 中的統一邏輯，提高可測試性與一致性。  
- **修改檔案**  
- 已有基礎的：`src/services/ai/workoutGenerator.js`、`src/services/ai/nutritionRecommendation.js`。  
- 待補/整理：  
- `src/services/ai/analysisService.js`（動作分析 AI）  
- `src/components/AICoach/CoachChat.jsx` → 抽到 `ai/coachService.js`  
- 各 View 中殘留的 prompt 組裝邏輯。  
- **預估影響面**  
- 會影響所有 AI 功能，出錯時使用者感受強烈，**中等風險**。  
- **完成標準**  
- [ ] 所有 View 不再直接呼叫 `runGemini`，一律透過對應的 ai service。  
- [ ] AI 回傳格式與錯誤 handling 一致，可由測試腳本或手動驗證。  
- [ ] 新增/更新 ai service 後，不需修改多個 View。  
- **自動化程度**  
- **多數需人工確認**  
- 可用搜尋協助找出 `runGemini` 啟用點，但抽象層次與參數設計需手動調整。  

### Phase 3：大型 View 精簡（高風險，需搭配測試清單）

- **目標**  
- 將 `CalendarView.jsx`、`StrengthAnalysisView.jsx`、`RunAnalysisView.jsx`、`DashboardView.jsx` 等大型檔案拆為「薄 View + service/hook + UI 子元件」。  
- **修改檔案**  
- 以 `CalendarView.jsx` 為例：  
- 抽出匯入相關 UI 至 `components/Calendar/ImportSection.jsx`（或多個元件）。  
- Calendar 本體只保留：選擇日期區間、控制 modal 狀態、呼叫已完成的 service/hook。  
- 分別對 Strength / Run Analysis、Dashboard 採取相同模式。  
- **預估影響面**  
- 幾乎涵蓋所有主要頁面，**屬高風險改動**。  
- **完成標準**  
- [ ] 每個大型 View 的行數降至 ~200–300 行，且閱讀時只需關心畫面流程。  
- [ ] 所有既有功能（Calendar CRUD、拖曳、AI 生成、匯入分析、統計圖表）在重構後仍全部可用。  
- [ ] 搭配一份明確的手動驗證 checklist（可取自 `ARCHITECTURE_DIAGNOSIS.md` / `REFACTOR_TODO.md` 中的驗證條目）。  
- **自動化程度**  
- **不適合自動重構，強烈建議人工逐步重構 + 驗證**  
- 建議每次只處理一個 View，完成後再進下一個，並善用 Git 分支與小步驟 commit。  

---

## 九、UI / 美術設計優化 TODO 清單（風格探索）

> 說明：以下是針對 My AI Coach 的介面風格建議，偏向 2025–2026 年常見的健身 / 資料儀表板 / AI 工具趨勢。
> 每一種風格都轉成可以分階段實作的 TODO，方便之後挑選 1–2 種作為主題或子主題。

### 🎨 Style 1：Modern Fitness Dark（現代健身深色儀表板）

- **目標**  
- 打造以「深色為主、強調數據可讀性」的健身儀表板風格，整體類似 Strava / Garmin Connect 深色主題，搭配亮色重點（藍、綠、橘）凸顯關鍵 KPI 與 AI 建議區塊。  
- **修改檔案**  
- `tailwind.config.js`：  
- 定義 `primary`, `accent`, `success`, `danger` 等色票（以深藍 / 青綠為主）。  
- `src/index.css` / `src/App.css`：  
- 設定全域背景（例如 `bg-slate-950`）、字體（採用更具科技感的字型，如 Inter / SF Pro）。  
- 共用 UI：  
- `src/components/Dashboard/StatCard.jsx`  
- `src/components/common/Skeleton.jsx`  
- `src/components/common/ErrorToast.jsx`  
- 主要視圖（先挑 1–2 個試作）：  
- `src/views/DashboardView.jsx`  
- `src/views/TrainingDashboardView.jsx`  
- **預估影響面**  
- 以「外觀」為主，不改資料流與邏輯，**功能風險低**；但因為影響多數頁面外觀，視覺改動體感強烈。  
- 需注意對比度與可讀性（特別是圖表與文字顏色）。  
- **完成標準**  
- [ ] 有一組明確的 Tailwind 色系（primary / secondary / accent）並被 Dashboard 類元件統一使用。  
- [ ] Dashboard / TrainingDashboard 的卡片、背景、文字在深色主題下皆具良好對比（符合基本 AA 對比度）。  
- [ ] 手機 / 平板 / 桌面三種寬度下，深色背景與卡片陰影呈現都不破版。  
- [ ] 使用者在深色主題下長時間閱讀圖表不會有明顯疲勞感。  
- **自動化程度**  
- **部分可自動重構，多數需人工設計確認**  
- 可以用搜尋集中替換顏色 class（例如 `bg-gray-800` → `bg-slate-900`），但實際色彩平衡與對比須人工微調。  

### 🎨 Style 2：Glassmorphism AI Panel（AI 教練磨砂玻璃風）

- **目標**  
- 針對 AI 教練與右下角聊天視窗，採用 Glassmorphism（磨砂玻璃）風格，營造「未來感 + AI 感」，與主 Dashboard 的實心卡片形成視覺層級差異。  
- **修改檔案**  
- `src/components/AICoach/CoachChat.jsx`：  
- 調整外框為半透明背景（`bg-white/10` 或 `bg-slate-800/40`）、`backdrop-blur-md`、細邊框（`border-white/10`）。  
- `src/layouts/MainLayout.jsx`：  
- 為 Chat 浮動按鈕與展開後的 Panel 預留安全區（避免蓋住行事曆重要內容）。  
- 全域樣式：  
- 在 `index.css` 中加上 glass 卡片的通用 class（例如 `.glass-panel`）以便複用。  
- **預估影響面**  
- 僅影響 AI 聊天相關 UI 與局部 layout，**功能風險極低**。  
- 視覺上會非常明顯，因此要注意與主題色的協調。  
- **完成標準**  
- [ ] CoachChat 展開視窗具備明顯的玻璃感（半透明 + 模糊 + 光暈邊框），與主內容區域有清楚層次。  
- [ ] 在深色背景下，泡泡訊息與文字依舊清晰可讀。  
- [ ] Chat 開關按鈕在桌機與手機上位置合理，不擋住底部導航或重要按鈕。  
- [ ] 實際在多種瀏覽器測試（Chrome / Edge / Safari），`backdrop-filter` 表現可接受。  
- **自動化程度**  
- **偏向人工設計調整**  
- 需要多次微調透明度、模糊度與主題色搭配，不建議單純透過大量自動替換完成。  

### 🎨 Style 3：Bento Grid Dashboard（便當盒式模組化卡片）

- **目標**  
- 將 Dashboard 的各類資訊（今日課表、PR、Zone2、AI 建議、裝備狀態）重新排版成具「Bento Grid」感的模組化卡片，讓內容一目瞭然，適合截圖分享。  
- **修改檔案**  
- `src/views/DashboardView.jsx`：  
- 重構頂部與中間區塊的排版，使用 CSS Grid 形成「1 大 + 2 中 + 3 小」的卡片組合。  
- 卡片元件：  
- `src/components/Dashboard/StatCard.jsx`  
- `src/components/Dashboard/AchievementPanel.jsx`  
- `src/components/Dashboard/PRTracker.jsx`  
- 可選：新增一個專用的 `DashboardSectionCard` 共用樣式元件。  
- **預估影響面**  
- 僅改排版與卡片尺寸，不修改資料來源與邏輯，**中低風險**。  
- 需要額外注意在手機螢幕的排列（Bento Grid 在小螢幕要退化成線性列表）。  
- **完成標準**  
- [ ] 在桌機寬度下，Dashboard 呈現 3–4 欄，形成有節奏感的卡片拼圖（非純等寬網格）。  
- [ ] 在平板與手機上，卡片自動重排為 1–2 欄，仍保持閱讀順序合理。  
- [ ] 今日課表 / PR / Zone2 / 最近 AI 建議各自佔有清晰區塊，不互相擠壓。  
- [ ] 使用者能在 3 秒內掃描出「今天要做什麼 + 最近進步什麼」。  
- **自動化程度**  
- **需要人工規劃排版，少量可借助 Tailwind 抽共用 class**  
- CSS Grid 結構與卡片大小配置需手動設計。  

### 🎨 Style 4：Minimal Health OS（極簡健康作業系統風）

- **目標**  
- 將 Profile、Nutrition、Calendar 等非 Dashboard 頁面，往「Health OS / Apple Fitness」那種極簡、留白多、邊界柔和的風格靠攏，讓介面更專注於內容本身。  
- **修改檔案**  
- `src/views/Profile` 下各子元件（`ProfileHeader.jsx`, `BodyDataForm.jsx`, 等）：  
- 降低邊框密度，改用柔和陰影與更多留白。  
- `src/views/NutritionView.jsx` / `src/views/CalendarView.jsx`：  
- 清理多餘裝飾線條與強烈底色，改為淺灰背景 + 清晰卡片。  
- 共用排版：  
- 在 `MainLayout.jsx` 增加「內容區 max-width + 居中」的樣式，避免全螢幕鋪滿造成密度過高。  
- **預估影響面**  
- 對使用者體感改變較大，但大多是 CSS 級別調整，**功能風險低**。  
- 需與深色 Dashboard 風格協調（可能採「深色 Dashboard + 較淺的內容頁」對比方案）。  
- **完成標準**  
- [ ] Profile / Nutrition / Calendar 的主要背景色與卡片風格一致，沒有「每頁一種 style」的割裂感。  
- [ ] 文字階層（標題 / 小標 / 內文）有明確字級與字重規則。  
- [ ] 在 13 吋筆電螢幕上，單頁內容視覺密度明顯降低，閱讀更輕鬆。  
- **自動化程度**  
- **多為人工設計微調**  
- 可以透過搜尋統一替換一些顏色或 padding，但版面與留白的拿捏仍需實際眼睛看。  

---

> 建議實作順序：
> 1. 先從 **Style 1（Modern Fitness Dark）** 做「色系與基礎深色主題」 → 建立統一顏色語言。
> 2. 接著在 AI 區塊導入 **Style 2（Glassmorphism AI Panel）**，強化 AI 特色。
> 3. 再視時間將 Dashboard 排版升級為 **Style 3（Bento Grid）**，最後再逐頁推進到 **Style 4（Minimal Health OS）**。