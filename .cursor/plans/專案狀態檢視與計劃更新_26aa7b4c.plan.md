---
name: ""
overview: ""
todos: []
isProject: false
---

# 專案狀態檢視與計劃更新

## 三、未來擴展功能（待規劃）

### 3.1 多平台 API 同步

**功能描述**:

- 透過 Strava、Garmin Connect、Apple Health 官方 API 自動同步資料
- 需要申請各平台 API 權限和 OAuth 流程

**實現要點**:

- 擴展 `src/services/import/platformSync.js` 的 `syncFromPlatformAPI()` 函數
- 實現 OAuth 授權流程
- 添加同步設定頁面
- 需要申請各平台 API 權限

**預期收益**: 提高資料匯入便利性，實現自動同步

---

### 3.2 PDF 報告生成

**功能描述**:

- 使用 jsPDF 生成訓練報告 PDF
- 需要安裝 jsPDF 依賴

**實現要點**:

- 安裝 `jspdf` 套件
- 擴展 `src/utils/reportGenerator.js` 添加 PDF 生成功能
- 在分享選單中添加「下載 PDF 報告」選項

**預期收益**: 提供更專業的報告格式

---

### 3.3 定期備份提醒

**功能描述**:

- 定期提醒用戶備份資料
- 可設定提醒頻率

**實現要點**:

- 在 `backupService.js` 中添加提醒邏輯
- 使用瀏覽器通知 API 或應用內提醒
- 記錄最後備份時間

**預期收益**: 提升用戶資料安全性

---

## 四、技術債務與優化

### 待改進項目

- 添加 E2E 測試（Playwright 或 Cypress）
- 完善 README.md 文檔
- TypeScript 遷移（可選）
- 性能監控和錯誤追蹤（Sentry）
- CI/CD 流程完善

---

## 五、實施建議

### 未來擴展項目

1. **多平台 API 同步** - 需要申請 API 權限和實現 OAuth 流程
2. **PDF 報告生成** - 需要安裝 jsPDF 套件
3. **定期備份提醒** - 可選功能，提升用戶體驗

---

## 六、未來專業化功能（待規劃）

以下兩個方向可進一步提升產品的專業度與 AI 智能深度，建議作為下一階段演進目標。

### 6.1 比賽配速策略計算器（Race Strategy Generator）

**理由（Math/Performance）**：

- 針對「半馬破 2」這類目標，單純的平均配速（Average Pace）是不夠的
- 實務上會採用 Negative Split（負分段）、補給時機、賽道起伏來調整策略

**功能描述**：

- **輸入**：
- 目標成績（例如：`1:59:00`）
- 賽道類型（平路 / 起伏）
- 可選：個人 FTP / 閾值配速、歷史比賽成績
- **輸出策略**：
- Negative Split（負分段）策略：
- 例如：前 10K 配速 5:45/km，後 11K 配速 5:35/km
- 可根據使用者耐力曲線與過往訓練自動微調
- 補給策略：
- 結合現有營養模組，在時間軸上標記「能量膠攝取點」
- 例如：比賽第 45 分鐘、第 90 分鐘，自動對應到距離與補給提醒
- **輸出格式**：
- 可視化表格：每 1K / 每 5K 的目標時間、累積時間、區間配速
- 圖表：配速隨距離變化的折線圖，標出關鍵分段
- 「配速手環」PDF：
- 產生可列印的配速條（每公里目標時間），方便跑者剪下貼在手上

**實現要點**：

- 在 `src/utils/workoutCalculations.js` 中新增配速策略計算函數
- 在 `TrainingPlanView` 中增加「比賽策略」子頁或彈窗，讓使用者輸入目標成績與賽道資訊
- 使用現有報表模組（`reportGenerator.js`）生成 PDF 或圖片版配速手環
- 未來可與 Calendar 整合，在比賽日前自動顯示「比賽配速策略」卡片

**預期收益**：

- 從「訓練計畫」延伸到「比賽日策略」，讓產品覆蓋完整訓練週期
- 結合現有營養模組，提供更貼近實戰的補給建議
- 提升對中高階跑者與教練的吸引力

---

### 6.2 RAG 個人化知識庫（Retrieval-Augmented Generation）

**理由（AI Engineering）**：

- 目前 Prompt Context 受限於 token 數量，無法長期記住使用者三個月前的日記、傷痛史、物理治療建議
- 若能結合 RAG（檢索增強生成），AI 教練可以在回答問題時「回顧」過去所有重要紀錄，提升連續性與信任感

**功能描述**：

- **資料來源**：
- 訓練日記（例如：Calendar 備註欄位）
- 傷痛紀錄（例如：特定 tag 或 Injury log）
- 物理治療／復健建議（使用者自填或教練輸入）
- **技術架構**：
- 引入向量搜尋（Vector Search）：
- 可選：Firebase Vector Search 擴充功能、Pinecone、Supabase Vector 等
- 將上述文字內容轉為 Embeddings（向量）並儲存
- **使用場景**：
- 當使用者問 AI：
- 例：「我左膝蓋最近有點怪怪的」
- AI 不只看本週數據，還會檢索：
- 「你兩個月前的 10/15 也有紀錄過左膝外側緊繃，當時是因為跑坡訓練過多……」
- 回覆中自動引用過往紀錄片段，讓回答具備「記憶溫度」

**實現要點**：

- 在 `aiContextService` / `contextManager` 中擴展一層 RAG 流程：
- 先用關鍵字 / Embedding 檢索出最相關的 N 筆歷史紀錄
- 再將這些紀錄作為 Context 傳給 Gemini
- 新增 `ai/knowledgeBaseService.js`（或類似命名）：
- 負責寫入 / 更新 / 刪除 Embeddings
- 封裝向量搜尋邏輯，避免洩漏具體供應商細節（Firebase / Pinecone 可替換）
- UI 層新增：
- 在教練聊天視窗中標註「本次回答參考了以下歷史紀錄：…」
- 在 Profile 或設定中，提供「匯出 / 刪除個人知識庫」的隱私控制選項

**預期收益**：

- 讓 AI 教練具備長期記憶能力，而不受單次 Prompt token 限制
- 在傷痛管理、長期計畫調整、心理狀態追蹤等情境中，提供更貼近真人教練的體驗
- 打造差異化賣點，讓產品從「通用健身 AI」進化為「真正懂你的私人教練」

---

## 七、重構 TODO 清單（以安全 Quick Wins 優先）

> 說明：本清單從「不破壞現有功能」出發，先做 3 個最安全、最有感的 Quick Wins，再列出後續可分階段執行的任務。
> 每項都標註是否適合 **自動重構**，或需較多 **人工確認**。

---

### 🟢 Quick Win 1：完善工具庫並移除重複定義

**目標**

- 確認 `utils/date.js`、`utils/number.js`、`utils/aiJson.js` 已完成，檢查並移除 View 內部的重複工具函數定義  
- 統一 `importHelpers.js` 的函數命名和組織，確保所有工具函數來源單一

**修改檔案**

- ✅ 已存在：`src/utils/date.js`（`formatDate`, `getWeekDates`）  
- ✅ 已存在：`src/utils/number.js`（`cleanNumber`, `parsePaceToDecimal`）  
- ✅ 已存在：`src/utils/aiJson.js`（`parseLLMJson`）  
- 🔧 檢查並修改：  
- `src/views/CalendarView.jsx` - 確認已使用 `utils/date.js`、`utils/number.js`  
- `src/views/TrendAnalysisView.jsx` - 確認已使用統一工具函數  
- `src/utils/importHelpers.js` - 移除重複定義，改用 import  
- 其他 View 檔案（透過 grep 搜尋重複定義）

**預估影響面**

- 影響檔案數：3–5 個（依實際搜尋結果為準）  
- 主要變動為「替換重複函數定義 → 改成 import」，邏輯不變  
- **功能風險**：⭐ (極低)

**完成標準**

- 全域搜尋 `formatDate(`、`getWeekDates(`、`cleanNumber(` 與 `parsePaceToDecimal(`，皆來自 `utils/date` 或 `utils/number`  
- 全域搜尋 `parseLLMJson` 或類似的 JSON 解析邏輯，皆使用 `utils/aiJson.js`  
- 刪除各 View / utils 內部重複定義的同名函數  
- Calendar / TrendAnalysis 等頁面的日期顯示、配速計算與匯入流程仍正確無誤  
- 瀏覽器 console 無新增錯誤或警告

**自動化程度**

- **🤖 適合自動重構 + 人工驗證**  
- 建議流程：

1. 使用 `grep` 搜尋重複定義的模式
2. 確認 `utils/*.js` 中的函數已涵蓋所有用例
3. 用「全專案搜尋替換」把舊定義刪掉、改為 import
4. 人工跑一輪關鍵頁面（Calendar / TrendAnalysis）確認行為

---

### 🟢 Quick Win 2：完成 API Key 管理遷移

**目標**

- 完成 `apiKeyService.js` 的遷移，替換所有殘留的 `localStorage.getItem('gemini_api_key')`  
- 建立 `hooks/useApiKey.js` Hook，提供響應式 API Key 管理

**修改檔案**

- ✅ 已存在：`src/services/apiKeyService.js`（`getApiKey()`, `setApiKey(key)`, `hasApiKey()`）  
- 🆕 新增：`src/hooks/useApiKey.js` - 回傳 `{ apiKey, setApiKey, hasApiKey, isLoading }`  
- 🔧 修改（將直接存取 localStorage 改為呼叫 service/hook）：  
- `src/components/AICoach/CoachChat.jsx`  
- `src/services/ai/workoutGenerator.js`  
- `src/services/ai/nutritionRecommendation.js`  
- `src/views/StrengthAnalysisView.jsx`（若仍有直接呼叫）  
- `src/views/RunAnalysisView.jsx`（若仍有直接呼叫）  
- `src/views/NutritionView.jsx`（若仍有直接呼叫）  
- 其他使用 Gemini 的 View 或 services（透過 grep 搜尋 `gemini_api_key`）

**預估影響面**

- 影響檔案數：約 4–8 個（依實際搜尋 `gemini_api_key` 決定）  
- 實際行為保持不變，重點在集中邏輯  
- **功能風險**：⭐⭐ (低) - 中低風險但收益大

**完成標準**

- 全域搜尋 `localStorage.getItem('gemini_api_key')` 結果為 0  
- 全域搜尋 `localStorage.setItem('gemini_api_key'` 結果為 0（統一使用 `setApiKey()`）  
- 所有服務改用 `apiKeyService` 或 `useApiKey` 取得 API Key  
- `hooks/useApiKey.js` 已建立並可正常使用  
- AI 教練、AI 課表、營養建議、動作分析等功能均可正常呼叫 Gemini  
- API Key 遺失或錯誤時，錯誤處理訊息一致且可透過 `ErrorToast` 顯示

**自動化程度**

- **🤖 部分可自動重構，多數需人工確認**  
- 建議流程：

1. `localStorage.getItem('gemini_api_key')` 可用全專案搜尋替換成 `getApiKey()`
2. 但每個呼叫點的錯誤處理、UI 交互（例如在沒有 Key 時顯示提示）需人工檢查與調整
3. 建立 `useApiKey` Hook，測試響應式更新是否正常

---

### 🟢 Quick Win 3：確認 AI JSON 解析統一使用

**目標**

- 確認所有 AI service 都使用 `utils/aiJson.js` 的 `parseLLMJson` 函數  
- 移除各 AI service 中重複的 JSON 解析邏輯

**修改檔案**

- ✅ 已存在：`src/utils/aiJson.js`（`parseLLMJson(raw, { rootType: 'object' | 'array' })`）  
- 🔧 檢查並修改（把內建的字串處理/parse 改用 helper）：  
- `src/services/ai/workoutGenerator.js`  
- `src/services/ai/nutritionRecommendation.js`  
- `src/services/ai/formCorrection.js`  
- `src/components/AICoach/CoachChat.jsx`（若有結構化輸出）  
- 其他會從 LLM 讀 JSON 的 services（透過 grep 搜尋 `replace(/```json` 或 `JSON.parse`）

**預估影響面**

- 影響檔案數：3–6 個 AI service  
- 功能不變，但若 helper 寫錯會影響多處  
- **功能風險**：⭐⭐ (低) - 需要一次寫好 + 驗證

**完成標準**

- 所有 AI service 不再自行 `replace(/```json/g, '')` / `substring(startIndex, endIndex + 1)`，改由 `parseLLMJson` 處理  
- 全域搜尋 `JSON.parse` 在 AI service 中的使用，確認都有使用 `parseLLMJson` 包裝  
- 在 AI 回傳正常 JSON、包在 ```json 區塊、或有前後雜訊文案時都能成功 parse  
- 當解析失敗時，會統一丟出可被 `handleError` 捕捉的錯誤（含 context）  
- 實測：AI 生成功能，比對重構前後輸出的結構與欄位一致

**自動化程度**

- **👤 不適合完全自動重構，需要人工確認**  
- 建議流程：

1. 雖可用搜尋找出共通 pattern（`replace(/```json`, `JSON.parse`）
2. 但每支 service 的輸出格式與期待 rootType 可能不同
3. 人工逐檔調整，並針對每支功能做一次實際呼叫測試
4. 確保 `parseLLMJson` 的錯誤處理覆蓋所有邊界情況

---

## 八、後續階段任務（依優先度拆分）

> 以下任務對應架構診斷報告中的重構路線圖，建議按「影響最大 → 風險可控」的順序執行。
> 每項任務都包含：**目標**、**修改檔案**、**預估影響面**、**完成標準**、**自動化程度**。

---

## 階段一：基礎設施完善與低風險遷移（2-3 天）

**整體目標**：完善工具庫，統一配置管理，建立 API 層骨架

---

### 任務 1-1：抽離 PR 服務至獨立模組

**目標**

- 從 `calendarService.js` 抽出 PR 追蹤相關邏輯（`extractPRs`、`getAllPRs`、`calculate1RM`）至獨立服務  
- 讓 `calendarService.js` 只負責基本的 CRUD 和訂閱，複雜統計邏輯集中管理

**修改檔案**

- 🆕 新增：`src/services/workout/prService.js`  
- 包含 `extractPRs(workouts)`、`getAllPRs()`、`calculate1RM(weight, reps)`  
- 🔧 修改：`src/services/calendarService.js`  
- 移除 `extractPRs`、`getAllPRs`、`calculate1RM` 函數定義  
- 保留基本的 CRUD 和訂閱功能  
- 🔧 修改：`src/views/DashboardView.jsx`、`src/components/Dashboard/PRTracker.jsx`  
- 改用 `prService.js` 的函數

**預估影響面**

- 影響檔案數：3 個（calendarService、DashboardView、PRTracker）  
- 不影響資料結構，只移動函數定義  
- **功能風險**：⭐⭐ (低)

**完成標準**

- `src/services/workout/prService.js` 已建立，包含所有 PR 相關函數  
- `calendarService.js` 中不再包含 PR 計算邏輯  
- Dashboard 的 PR 顯示正常  
- 重構前後，對同一帳號的 PR 資訊數值一致

**自動化程度**

- **👤 需要人工確認**  
- 屬於 domain 切分與邏輯移轉，建議逐步搬移並搭配手動驗證

---

### 任務 1-2：建立 API 層骨架（與現有服務並行）

**目標**

- 建立 `src/api/` 目錄，封裝 Firebase 呼叫，為後續統一資料存取層鋪路  
- 與現有服務並行運作，不影響現有功能

**修改檔案**

- 🆕 新增：`src/api/firebase.js` - 重新匯出 `firebase.js`（保持相容）  
- 🆕 新增：`src/api/auth.js` - 封裝認證服務（從 `authService.js` 遷移）  
- 🆕 新增：`src/api/workouts.js` - 封裝訓練資料 CRUD（從 `calendarService.js` 抽象）  
- 🆕 新增：`src/api/nutrition.js` - 營養紀錄 CRUD 骨架  
- 🆕 新增：`src/api/body.js` - 身體數據 CRUD 骨架  
- 🆕 新增：`src/api/gears.js` - 裝備管理 CRUD 骨架

**預估影響面**

- 新增檔案數：6 個（純新增，不修改現有檔案）  
- 與現有服務並行運作，不影響功能  
- **功能風險**：⭐ (極低) - 僅新增骨架檔案

**完成標準**

- `src/api/` 目錄已建立，包含所有骨架檔案  
- `api/firebase.js` 正確匯出 Firebase 實例  
- `api/auth.js` 提供 `signInWithGoogle`、`signOut` 等函數（與 `authService.js` 功能一致）  
- `api/workouts.js` 提供基本的 CRUD 函數（與 `calendarService.js` 功能對應）  
- 現有功能不受影響（API 層尚未被使用）

**自動化程度**

- **🤖 適合自動生成骨架**  
- 建議：先建立檔案結構和函數簽名，後續逐步實作內容

---

## 階段二：業務邏輯抽離與服務層建立（5-7 天）

**整體目標**：將業務邏輯從 View 移至 Services，建立響應式資料流

---

### 任務 2-1：抽離檔案解析服務

**目標**

- 從 `CalendarView.jsx` 抽出 FIT/CSV 檔案解析邏輯至獨立服務  
- 讓 View 專注於 UI 互動，檔案處理邏輯集中管理

**修改檔案**

- 🆕 新增：`src/services/import/fitParser.js`  
- 從 `CalendarView.jsx` 抽出 `handleFitAnalysis` 邏輯  
- 提供 `parseFITFile(file)` 函數  
- 🆕 新增：`src/services/import/csvParser.js`  
- 從 `CalendarView.jsx` 抽出 `handleCSVUpload` 邏輯  
- 提供 `parseCSVFile(file)` 函數  
- 🔧 修改：`src/views/CalendarView.jsx`  
- 移除 FIT/CSV 解析邏輯  
- 改用 `fitParser.js`、`csvParser.js` 的函數

**預估影響面**

- 影響檔案數：3 個（CalendarView + 2 個新服務）  
- 不影響資料結構和儲存方式  
- **功能風險**：⭐⭐ (低)

**完成標準**

- `fitParser.js`、`csvParser.js` 已建立  
- CalendarView 中不再包含檔案解析邏輯  
- FIT 檔案匯入功能正常（Garmin/Strava 格式）  
- CSV 檔案匯入功能正常  
- 錯誤處理一致且可透過 `ErrorToast` 顯示

**自動化程度**

- **👤 需要人工確認**  
- 建議：先將邏輯複製到新檔案，測試通過後再從 CalendarView 移除

---

### 任務 2-2：完善 AI 服務（抽離 CoachChat 邏輯）

**目標**

- 從 `CoachChat.jsx` 抽出對話邏輯至 `services/ai/coachService.js`  
- 建立 `services/ai/prompts/` 目錄，統一管理 Prompt 模板  
- 從 `StrengthAnalysisView.jsx`、`RunAnalysisView.jsx` 抽出 AI 分析邏輯

**修改檔案**

- ✅ 已存在：`src/services/ai/workoutGenerator.js`  
- ✅ 已存在：`src/services/ai/nutritionRecommendation.js`  
- 🆕 新增：`src/services/ai/coachService.js`  
- 從 `CoachChat.jsx` 抽出對話邏輯  
- 提供 `sendMessage(message, context)` 函數  
- 🆕 新增：`src/services/ai/prompts/coachPrompts.js` - 教練對話 Prompt 模板  
- 🆕 新增：`src/services/ai/prompts/analysisPrompts.js` - 動作分析 Prompt 模板  
- 🆕 新增：`src/services/ai/analysisService.js` - 動作分析 AI 服務  
- 🔧 修改：`src/components/AICoach/CoachChat.jsx`  
- 改用 `coachService.js`，只保留 UI 邏輯  
- 🔧 修改：`src/views/StrengthAnalysisView.jsx`、`src/views/RunAnalysisView.jsx`  
- 改用 `analysisService.js`，移除 Prompt 組裝邏輯

**預估影響面**

- 影響檔案數：約 5–6 個（新增 4 個 + 修改 2–3 個）  
- 會影響所有 AI 功能，出錯時使用者感受強烈  
- **功能風險**：⭐⭐⭐ (中)

**完成標準**

- `coachService.js`、`analysisService.js` 已建立  
- `prompts/` 目錄已建立，包含所有 Prompt 模板  
- 所有 View 不再直接呼叫 `runGemini`，一律透過對應的 ai service  
- AI 教練對話功能正常  
- AI 動作分析功能正常（重訓/跑步）  
- AI 回傳格式與錯誤 handling 一致  
- 新增/更新 ai service 後，不需修改多個 View

**自動化程度**

- **👤 多數需人工確認**  
- 可用搜尋協助找出 `runGemini` 啟用點，但抽象層次與參數設計需手動調整

---

### 任務 2-3：建立 MediaPipe 分析服務

**目標**

- 從 `StrengthAnalysisView.jsx`、`RunAnalysisView.jsx` 抽出 MediaPipe 姿態分析邏輯  
- 抽出評分計算邏輯，讓 View 專注於 UI 互動

**修改檔案**

- 🆕 新增：`src/services/analysis/poseAnalysis.js`  
- 封裝 MediaPipe 姿態分析邏輯  
- 提供 `analyzePose(video)` 函數  
- 🆕 新增：`src/services/analysis/metricsCalculator.js`  
- 抽出評分計算邏輯  
- 提供 `calculateFormScore(poseData)` 函數  
- 🔧 修改：`src/views/StrengthAnalysisView.jsx`  
- 改用 `poseAnalysis.js`、`metricsCalculator.js`  
- 移除 MediaPipe 和評分計算邏輯  
- 🔧 修改：`src/views/RunAnalysisView.jsx`  
- 改用 `poseAnalysis.js`、`metricsCalculator.js`  
- 移除 MediaPipe 和評分計算邏輯

**預估影響面**

- 影響檔案數：4 個（新增 2 個 + 修改 2 個）  
- 不影響資料儲存，只移動邏輯  
- **功能風險**：⭐⭐⭐ (中) - 複雜的姿態分析邏輯

**完成標準**

- `poseAnalysis.js`、`metricsCalculator.js` 已建立  
- StrengthAnalysisView、RunAnalysisView 中不再包含 MediaPipe 邏輯  
- 姿態偵測功能正常（重訓/跑步動作）  
- 評分計算準確  
- AI 分析建議正常  
- 儲存功能正常

**自動化程度**

- **👤 需要人工確認**  
- 建議：保留原始邏輯作為註解，對比抽離前後的結果

---

### 任務 2-4：建立響應式 Hooks ✅ **已完成**

**目標**

- 建立 `useWorkouts`、`useNutrition`、`useGears` 等 Hooks，封裝資料訂閱邏輯  
- ~~建立 `workoutStore.js`，實現響應式資料流，資料變更時 UI 自動更新~~（暫緩，先使用 Hooks）

**修改檔案**

- ✅ 新增：`src/hooks/useWorkouts.js`  
- 封裝訓練資料訂閱，提供 `useCompletedWorkouts()`, `useAllWorkouts()`, `useWorkoutsByDateRange()`, `useTodayWorkouts()`  
- 自動處理載入狀態和錯誤  
- ✅ 新增：`src/hooks/useNutrition.js`  
- 封裝營養紀錄訂閱，提供 `useNutrition(date)`  
- ✅ 新增：`src/hooks/useGears.js`  
- 封裝裝備管理，提供 `useGears()`  
- ✅ 修改：`src/views/DashboardView.jsx`  
- 改用 `useTodayWorkouts` Hook，移除手動 `fetchTodaySchedule`  
- ✅ 修改：`src/views/NutritionView.jsx`  
- 改用 `useNutrition` Hook，移除手動 `subscribeFoodLogsByDate` 與 summary 計算  
- ✅ 修改：`src/views/GearView.jsx`  
- 改用 `useGears` Hook，移除手動 `subscribeGears`

**預估影響面**

- 影響檔案數：約 5–6 個（新增 4 個 + 修改 2–3 個）  
- 會改變資料更新機制，需確保訂閱正確取消  
- **功能風險**：⭐⭐⭐ (中) - 狀態管理變更，可能導致 UI 不更新

**完成標準**

- ✅ `useWorkouts`、`useNutrition`、`useGears` Hooks 已建立  
- ✅ Dashboard、Nutrition、Gear 等頁面改用新 Hooks  
- ✅ 資料更新時 UI 自動刷新（實時訂閱）  
- ✅ 組件卸載時正確取消訂閱（useEffect cleanup）  
- 多分頁同步正常

**自動化程度**

- **👤 需要人工確認**  
- 建議：使用 React DevTools 檢查狀態更新，測試快速切換頁面

---

### 任務 2-5：遷移至統一 API 層

**目標**

- 逐步將 `calendarService.js`、`nutritionService.js` 等改為使用 `api/*` 層  
- 統一錯誤處理、快取策略，讓資料存取層更清晰

**修改檔案**

- 🔧 修改：`src/api/workouts.js` - 完善實作，與 `calendarService.js` 功能對應  
- 🔧 修改：`src/api/nutrition.js` - 完善實作，與 `nutritionService.js` 功能對應  
- 🔧 修改：`src/api/body.js` - 完善實作，與 `bodyService.js` 功能對應  
- 🔧 修改：`src/services/calendarService.js`  
- 改用 `api/workouts.js` 的函數（內部實作）  
- 🔧 修改：`src/services/nutritionService.js`  
- 改用 `api/nutrition.js` 的函數（內部實作）

**預估影響面**

- 影響檔案數：約 5–6 個（修改 API 層 + Services 層）  
- 不影響 View 層，只改變 Service 內部實作  
- **功能風險**：⭐⭐⭐ (中) - 資料庫操作，錯誤會導致資料遺失

**完成標準**

- `api/workouts.js`、`api/nutrition.js`、`api/body.js` 已完成實作  
- Services 層改用 API 層的函數  
- 讀取/新增/更新/刪除功能正常  
- 實時訂閱正常（多分頁同步）  
- 離線模式正常（快取讀取）  
- 錯誤處理統一且一致

**自動化程度**

- **👤 需要人工確認**  
- 建議：保留原 Firebase 調用作為備份註解，對比重構前後的資料查詢結果

---

## 階段三：大型 View 精簡與架構收斂（5-7 天）

**整體目標**：大幅精簡 View 組件，移除業務邏輯，僅保留 UI 組合

---

### 任務 3-1：精簡 CalendarView.jsx（高風險）

**目標**

- 將 `CalendarView.jsx` 從 639 行降至 200-300 行  
- 移除所有業務邏輯，只保留 UI 組合和事件處理

**修改檔案**

- 🆕 新增：`src/components/Calendar/WorkoutCard.jsx` - 訓練卡片元件（可重用）  
- 🆕 新增：`src/components/Calendar/ImportSection.jsx` - 匯入相關 UI  
- 🔧 修改：`src/views/CalendarView.jsx`  
- 移除 FIT/CSV 解析邏輯（改用 `fitParser.js`、`csvParser.js`）  
- 移除 AI 生成邏輯（改用 `workoutGenerator.js`）  
- 移除日期/數值處理（改用 `utils/date.js`、`utils/number.js`）  
- 移除 Firebase 直接呼叫（改用 `useWorkouts` Hook）  
- 只保留：選擇日期區間、控制 modal 狀態、呼叫已完成的 service/hook

**預估影響面**

- 影響檔案數：3 個（新增 2 個 + 修改 1 個）  
- 幾乎涵蓋所有主要頁面功能  
- **功能風險**：⭐⭐⭐⭐ (高) - 大型組件修改，容易遺漏細節

**完成標準**

- CalendarView.jsx 行數降至 200-300 行  
- 所有既有功能在重構後仍全部可用：  
- 新增/編輯/刪除訓練紀錄  
- 拖曳移動訓練到不同日期  
- Ctrl+拖曳複製訓練  
- AI 生成單日課表（重訓/跑步）  
- AI 生成本週課表（多選模式）  
- 匯入 FIT 檔案（Garmin/Strava）  
- 匯入 CSV 檔案  
- 匯出 CSV 備份  
- 更新後 Dashboard 自動刷新  
- 切換月份顯示正常  
- 瀏覽器 console 無新增錯誤或警告

**自動化程度**

- **👤 不適合自動重構，強烈建議人工逐步重構 + 驗證**  
- 建議：每次只處理一個功能區塊，完成後立即測試，使用 Git 分支管理

---

### 任務 3-2：精簡 StrengthAnalysisView.jsx 與 RunAnalysisView.jsx

**目標**

- 將 `StrengthAnalysisView.jsx` 從 400+ 行降至 150-200 行  
- 將 `RunAnalysisView.jsx` 從 600+ 行降至 150-200 行  
- 移除所有業務邏輯，只保留 UI 組合

**修改檔案**

- 🔧 修改：`src/views/StrengthAnalysisView.jsx`  
- 移除 MediaPipe 邏輯（改用 `poseAnalysis.js`）  
- 移除評分計算（改用 `metricsCalculator.js`）  
- 移除 AI 分析邏輯（改用 `analysisService.js`）  
- 🔧 修改：`src/views/RunAnalysisView.jsx`  
- 移除 MediaPipe 邏輯（改用 `poseAnalysis.js`）  
- 移除評分計算（改用 `metricsCalculator.js`）  
- 移除 AI 分析邏輯（改用 `analysisService.js`）

**預估影響面**

- 影響檔案數：2 個  
- 不影響資料儲存，只改變 View 結構  
- **功能風險**：⭐⭐⭐⭐ (高) - 複雜的姿態分析功能

**完成標準**

- StrengthAnalysisView.jsx 行數降至 150-200 行  
- RunAnalysisView.jsx 行數降至 150-200 行  
- 所有既有功能在重構後仍全部可用：  
- 姿態偵測功能正常（重訓/跑步動作）  
- 評分計算準確  
- AI 分析建議正常  
- 儲存功能正常  
- 瀏覽器 console 無新增錯誤或警告

**自動化程度**

- **👤 不適合自動重構，強烈建議人工逐步重構 + 驗證**  
- 建議：保留原始邏輯作為註解，逐功能測試

---

### 任務 3-3：精簡 DashboardView.jsx

**目標**

- 將 `DashboardView.jsx` 從 500+ 行降至 200-250 行  
- 移除手動資料載入，改用響應式 Hooks

**修改檔案**

- ✅ 已使用 `workoutService.js` 取得統計  
- 🔧 修改：`src/views/DashboardView.jsx`  
- 移除手動 `fetchWorkoutStats`（改用響應式 `useWorkouts`）  
- 簡化狀態管理（改用 `workoutStore`）  
- 移除重複的日期/數值處理邏輯

**預估影響面**

- 影響檔案數：1 個  
- 不影響資料來源，只改變載入方式  
- **功能風險**：⭐⭐⭐ (中)

**完成標準**

- DashboardView.jsx 行數降至 200-250 行  
- 所有既有功能在重構後仍全部可用：  
- 統計數據顯示正常  
- 肌肉熱圖顯示正常  
- 今日課表顯示正常  
- PR 追蹤顯示正常  
- 成就面板顯示正常  
- 資料更新時 UI 自動刷新（不需要手動觸發）  
- 瀏覽器 console 無新增錯誤或警告

**自動化程度**

- **👤 需要人工確認**  
- 建議：逐步替換，測試每個功能區塊

---

### 任務 3-4：統一命名與組織

**目標**

- 統一檔案命名規範和函數命名規範  
- 整理 `services/` 目錄結構，移除根層服務檔案

**修改檔案**

- 🔧 整理：`src/services/` 目錄結構  
- 將根層服務檔案移至對應子目錄（如 `config/apiKeyService.js`）  
- 統一檔案命名（`.jsx` for components, `.js` for services/utils）  
- 🔧 整理：函數命名規範  
- 統一使用 camelCase，英文命名  
- 統一註解格式

**預估影響面**

- 影響檔案數：所有服務檔案  
- 只改變檔案位置和命名，不影響功能  
- **功能風險**：⭐⭐ (低) - 主要是重構工作

**完成標準**

- 檔案命名規範統一（`.jsx` for components, `.js` for services/utils）  
- 函數命名規範統一（camelCase，英文命名）  
- `services/` 目錄結構清晰，根層不再有散落的服務檔案  
- 所有 import 路徑已更新，功能正常  
- 專案結構清晰，易於導航

**自動化程度**

- **🤖 部分可自動重構**  
- 建議：使用 VS Code 的重構工具批量更新 import 路徑

---

## 九、UI / 美術設計優化 TODO 清單（風格探索）

> 說明：以下是針對 My AI Coach 的介面風格建議，偏向 2025–2026 年常見的健身 / 資料儀表板 / AI 工具趨勢。
> 每一種風格都轉成可以分階段實作的 TODO，方便之後挑選 1–2 種作為主題或子主題。

### 🎨 Style 1：Modern Fitness Dark（現代健身深色儀表板）✅ **部分完成**

- **目標**  
- 打造以「深色為主、強調數據可讀性」的健身儀表板風格，整體類似 Strava / Garmin Connect 深色主題，搭配亮色重點（藍、綠、橘）凸顯關鍵 KPI 與 AI 建議區塊。  
- **修改檔案**  
- ✅ `tailwind.config.js`：  
- 已定義 `primary`, `accent`, `danger`, `surface-800`, `surface-900` 等色票  
- ✅ `src/index.css`：  
- 已設定全域背景 `bg-surface-900`、自訂捲軸樣式  
- ✅ 共用 UI：  
- ✅ `src/components/Dashboard/StatCard.jsx` - 已更新為 `bg-surface-800`  
- ✅ `src/components/common/Skeleton.jsx` - 已更新為深色主題  
- ✅ 主要視圖：  
- ✅ `src/views/DashboardView.jsx` - 已使用新 Hooks  
- ✅ `src/views/TrainingDashboardView.jsx` - 已更新 UI 風格（`bg-surface-800`, `border-gray-800`, `bg-primary-500`）  
- ✅ `src/views/CalendarView.jsx` - 已更新 UI 風格  
- ✅ `src/views/NutritionView.jsx` - 已更新 UI 風格  
- ✅ `src/views/GearView.jsx` - 已更新 UI 風格  
- ✅ `src/layouts/MainLayout.jsx` - 已更新為 `bg-surface-900`  
- **預估影響面**  
- 以「外觀」為主，不改資料流與邏輯，**功能風險低**；但因為影響多數頁面外觀，視覺改動體感強烈。  
- 需注意對比度與可讀性（特別是圖表與文字顏色）。  
- **完成標準**  
- ✅ 已有一組明確的 Tailwind 色系（primary / accent / surface-800 / surface-900）並被多數元件統一使用  
- ✅ Dashboard / TrainingDashboard / Calendar / Nutrition / Gear 的卡片、背景、文字在深色主題下皆具良好對比  
- ⏳ 手機 / 平板 / 桌面三種寬度下，深色背景與卡片陰影呈現都不破版（待測試）  
- ⏳ 使用者在深色主題下長時間閱讀圖表不會有明顯疲勞感（待使用者回饋）  
- **自動化程度**  
- **部分可自動重構，多數需人工設計確認**  
- 可以用搜尋集中替換顏色 class（例如 `bg-gray-800` → `bg-slate-900`），但實際色彩平衡與對比須人工微調。

### 🎨 Style 2：Glassmorphism AI Panel（AI 教練磨砂玻璃風）

- **目標**  
- 針對 AI 教練與右下角聊天視窗，採用 Glassmorphism（磨砂玻璃）風格，營造「未來感 + AI 感」，與主 Dashboard 的實心卡片形成視覺層級差異。  
- **修改檔案**  
- `src/components/AICoach/CoachChat.jsx`：  
- 調整外框為半透明背景（`bg-white/10` 或 `bg-slate-800/40`）、`backdrop-blur-md`、細邊框（`border-white/10`）。  
- `src/layouts/MainLayout.jsx`：  
- 為 Chat 浮動按鈕與展開後的 Panel 預留安全區（避免蓋住行事曆重要內容）。  
- 全域樣式：  
- 在 `index.css` 中加上 glass 卡片的通用 class（例如 `.glass-panel`）以便複用。  
- **預估影響面**  
- 僅影響 AI 聊天相關 UI 與局部 layout，**功能風險極低**。  
- 視覺上會非常明顯，因此要注意與主題色的協調。  
- **完成標準**  
- CoachChat 展開視窗具備明顯的玻璃感（半透明 + 模糊 + 光暈邊框），與主內容區域有清楚層次。  
- 在深色背景下，泡泡訊息與文字依舊清晰可讀。  
- Chat 開關按鈕在桌機與手機上位置合理，不擋住底部導航或重要按鈕。  
- 實際在多種瀏覽器測試（Chrome / Edge / Safari），`backdrop-filter` 表現可接受。  
- **自動化程度**  
- **偏向人工設計調整**  
- 需要多次微調透明度、模糊度與主題色搭配，不建議單純透過大量自動替換完成。

### 🎨 Style 3：Bento Grid Dashboard（便當盒式模組化卡片）

- **目標**  
- 將 Dashboard 的各類資訊（今日課表、PR、Zone2、AI 建議、裝備狀態）重新排版成具「Bento Grid」感的模組化卡片，讓內容一目瞭然，適合截圖分享。  
- **修改檔案**  
- `src/views/DashboardView.jsx`：  
- 重構頂部與中間區塊的排版，使用 CSS Grid 形成「1 大 + 2 中 + 3 小」的卡片組合。  
- 卡片元件：  
- `src/components/Dashboard/StatCard.jsx`  
- `src/components/Dashboard/AchievementPanel.jsx`  
- `src/components/Dashboard/PRTracker.jsx`  
- 可選：新增一個專用的 `DashboardSectionCard` 共用樣式元件。  
- **預估影響面**  
- 僅改排版與卡片尺寸，不修改資料來源與邏輯，**中低風險**。  
- 需要額外注意在手機螢幕的排列（Bento Grid 在小螢幕要退化成線性列表）。  
- **完成標準**  
- 在桌機寬度下，Dashboard 呈現 3–4 欄，形成有節奏感的卡片拼圖（非純等寬網格）。  
- 在平板與手機上，卡片自動重排為 1–2 欄，仍保持閱讀順序合理。  
- 今日課表 / PR / Zone2 / 最近 AI 建議各自佔有清晰區塊，不互相擠壓。  
- 使用者能在 3 秒內掃描出「今天要做什麼 + 最近進步什麼」。  
- **自動化程度**  
- **需要人工規劃排版，少量可借助 Tailwind 抽共用 class**  
- CSS Grid 結構與卡片大小配置需手動設計。

### 🎨 Style 4：Minimal Health OS（極簡健康作業系統風）

- **目標**  
- 將 Profile、Nutrition、Calendar 等非 Dashboard 頁面，往「Health OS / Apple Fitness」那種極簡、留白多、邊界柔和的風格靠攏，讓介面更專注於內容本身。  
- **修改檔案**  
- `src/views/Profile` 下各子元件（`ProfileHeader.jsx`, `BodyDataForm.jsx`, 等）：  
- 降低邊框密度，改用柔和陰影與更多留白。  
- `src/views/NutritionView.jsx` / `src/views/CalendarView.jsx`：  
- 清理多餘裝飾線條與強烈底色，改為淺灰背景 + 清晰卡片。  
- 共用排版：  
- 在 `MainLayout.jsx` 增加「內容區 max-width + 居中」的樣式，避免全螢幕鋪滿造成密度過高。  
- **預估影響面**  
- 對使用者體感改變較大，但大多是 CSS 級別調整，**功能風險低**。  
- 需與深色 Dashboard 風格協調（可能採「深色 Dashboard + 較淺的內容頁」對比方案）。  
- **完成標準**  
- Profile / Nutrition / Calendar 的主要背景色與卡片風格一致，沒有「每頁一種 style」的割裂感。  
- 文字階層（標題 / 小標 / 內文）有明確字級與字重規則。  
- 在 13 吋筆電螢幕上，單頁內容視覺密度明顯降低，閱讀更輕鬆。  
- **自動化程度**  
- **多為人工設計微調**  
- 可以透過搜尋統一替換一些顏色或 padding，但版面與留白的拿捏仍需實際眼睛看。

---

> 建議實作順序：
>
> 1. 先從 **Style 1（Modern Fitness Dark）** 做「色系與基礎深色主題」 → 建立統一顏色語言。
> 2. 接著在 AI 區塊導入 **Style 2（Glassmorphism AI Panel）**，強化 AI 特色。
> 3. 再視時間將 Dashboard 排版升級為 **Style 3（Bento Grid）**，最後再逐頁推進到 **Style 4（Minimal Health OS）**。

